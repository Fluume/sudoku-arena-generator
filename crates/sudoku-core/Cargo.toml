[package]
name = "sudoku-core"
description = "Core Sudoku board representation, bitmask bookkeeping, and symmetry transforms"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rand_core = "0.6"
serde = { version = "1", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
