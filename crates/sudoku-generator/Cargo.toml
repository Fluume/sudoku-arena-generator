[package]
name = "sudoku-generator"
description = "Full-grid generation and pluggable puzzle construction strategies"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sudoku-core = { path = "../sudoku-core" }
sudoku-solver = { path = "../sudoku-solver" }
rand = "0.8"
rand_core = "0.6"
rayon = "1"
